# ── Virtual Environment ──
.RAGenv/
venv/
.venv/

# ── Large Data Files ──
*.npy
*.npz
*.h5
*.hdf5
sunny-rag/data*/vectors.npy
sunny-rag/data*/texts.jsonl
sunny-rag/data*/id_to_index.json

# ── Qdrant Data ──
data/qdrant_data/

# ── Chunked Data ──
data/chunked_data/

# ── Backups ──
backups/
*.tar.gz
*.zip

# ── Logs ──
logs/
*.log

# ── Sessions (runtime data) ──
server/**/sessions/
server/v4/sessions.json
server/v4/sessions.json.bak
server/v5/sessions.json

# ── Annotated PDFs (generated) ──
server/v4/annotated_pdfs/
server/v5/annotated_pdfs/

# ── Python Cache ──
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# ── IDE / Editor ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── Environment & Secrets ──
.env
.env.local
*.key

# ── OS ──
.DS_Store
Thumbs.db

# ── Backup files ──
*.bak
*.bak2
*.bak3
*.backup
*.pre_refactor

# ── Deprecated version markers ──
*.v2_bak

# ── Chrome Profile (data pipeline residue) ──
data/chrome_profile/

# ── Pipeline state ──
data/pipeline/

# ── Data Artifacts (intermediate) ──
data/extracted_texts/
data/chunked_v3/
data/chunked_v4/
data/chunked_v5/

# ── Node modules ──
node_modules/
deps/node_modules/

# ── Claude local config ──
.claude/

# ── Milvus / Vector DB runtime data ──
data/milvus_data/

# ── Legacy session data ──
data/perovskite_sessions/
